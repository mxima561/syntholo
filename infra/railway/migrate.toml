"$schema" = "https://railway.com/railway.schema.json"

[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/api/Dockerfile"
dockerfileTarget = "migration-runtime"

[deploy]
startCommand = "/usr/local/bin/node /app/migrate.js"
restartPolicyType = "NEVER"
